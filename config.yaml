path:
  data_dir: js-fakes/midi/*.mid
  test_dir: js-fakes/jsf-extended/*.mid
  model_dir: models/
  out_dir: outputs/
params:
  bpm: 120
  batch_size: 16
  epochs: 5000
  layers: 2
  unit: 512
  dropout: 0.2
  embed_size: 256
  sequence_length: 16
  stateful: True
  track_size: 4
output:
  wandb: False
  length: 50
